from django.conf.urls import *
from meadowspta.contrib.wishlist.views import *

urlpatterns = patterns('meadowspta.contrib.wishlist.views',
    # url(r'^search/$', search),
    # url(r'^product/(?P<product_id>.*)$', view_product),
)